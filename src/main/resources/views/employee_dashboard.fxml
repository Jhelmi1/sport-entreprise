<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="app.controllers.EmployeeDashboardController"
            style="-fx-background-color: white;">

    <top>
        <HBox spacing="16"
              alignment="CENTER_LEFT"
              style="-fx-background-color: white;
                     -fx-padding: 15 30 15 30;
                     -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.12), 12, 0.15, 0, 2);">

            <ImageView fitHeight="50" preserveRatio="true">
                <image>
                    <Image url="@/assets/lcss.png"/>
                </image>
            </ImageView>

            <Region HBox.hgrow="ALWAYS"/>

            <!-- NOTIFICATION ICON -->
            <StackPane onMouseClicked="#onNotificationsClick">

                <Label text="🔔" style="-fx-font-size: 18px;"/>

                <Label fx:id="notificationBadge"
                       style="-fx-background-color: #c62828;
                              -fx-text-fill: white;
                              -fx-font-size: 10px;
                              -fx-padding: 2 6 2 6;
                              -fx-background-radius: 999;"
                       visible="false"
                       StackPane.alignment="TOP_RIGHT"/>
            </StackPane>

            <!-- AVATAR -->
            <StackPane fx:id="avatar"
                       prefWidth="40" prefHeight="40"
                       style="-fx-background-radius: 999; -fx-background-color: #2e7d32;"
                       onMouseClicked="#onAvatarClick">

                <Label fx:id="avatarLabel"
                       text="??"
                       style="-fx-text-fill: white; -fx-font-weight: bold;"/>
            </StackPane>

        </HBox>
    </top>

    <center>
        <VBox spacing="20" alignment="CENTER">

            <Label text="Dashboard Employé"
                   style="-fx-font-size: 22px; -fx-font-weight: bold;"/>

            <Button text="Voir événements"
                    onAction="#onEvents"
                    prefWidth="220"/>

            <Button text="Mes inscriptions"
                    onAction="#onMyRegistrations"
                    prefWidth="220"/>

        </VBox>
    </center>

</BorderPane>
